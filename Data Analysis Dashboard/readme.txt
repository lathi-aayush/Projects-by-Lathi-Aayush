Steps to Run the file
Step 1 : Download the whole File named "Data Analysis Dashboard"
Step 2 : Open Command Prompt by right clicking in the folder path
Step 3 : enter this on command line : "streamlit run Waste_Management_Insights.py"


list of all the Libraries used for the dashboard

pip install streamlit 
pip install pandas 
pip install numpy
pip install matplotlib.pyplot
pip install seaborn
pip install plotly
pip install geopandas
