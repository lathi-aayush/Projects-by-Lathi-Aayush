list of all the Libraries used for the dashboard

pip install streamlit 
pip install pandas 
pip install numpy
pip install matplotlib.pyplot
pip install seaborn
pip install plotly
pip install geopandas